# Codex 任务清单

## Task 001：初始化 Python 项目

实现：

- pyproject.toml
- index_platform 包目录
- tests 目录
- pytest 配置
- 最小 CLI 模块
- README 基础说明

暂时不要实现真实金融逻辑。

验收标准：

- pytest 通过
- python -m index_platform.cli.main --help 可运行

## Task 002：添加指数注册表

实现：

- configs/indices.yaml
- index_platform/universe/registry.py
- 指数注册表读取测试

指数字段：

- symbol
- name
- market
- currency
- source

验收标准：

- 可以读取全部指数
- 可以按 market 过滤指数
- pytest 通过

## Task 003：添加价格数据 schema 和 CSV 适配器

实现：

- index_platform/data/schema.py
- index_platform/data/csv_adapter.py
- index_platform/data/validators.py
- 测试

日频价格必须包含字段：

- date
- symbol
- open
- high
- low
- close
- volume
- amount
- currency
- source

验收标准：

- 可以读取 CSV
- 可以标准化 date 字段
- 可以校验必需字段
- pytest 通过

## Task 004：添加 Parquet 本地存储

实现：

- index_platform/storage/parquet_store.py
- save_price_data
- load_price_data
- 测试

验收标准：

- 可以保存价格数据
- 可以读取价格数据
- 支持 symbol、start、end 过滤
- pytest 通过

## Task 005：实现买入持有回测

实现：

- index_platform/strategy/base.py
- index_platform/strategy/buy_hold.py
- index_platform/backtest/engine.py
- 测试

验收标准：

- 给定简单价格序列，NAV 计算正确
- 不存在未来函数
- pytest 通过

## Task 006：添加绩效指标

实现：

- index_platform/metrics/performance.py
- 测试

指标：

- total_return
- annualized_return
- annualized_volatility
- max_drawdown
- sharpe_ratio
- calmar_ratio

验收标准：

- 指标在 toy data 上结果正确
- pytest 通过

## Task 007：实现 CLI 命令

实现：

- idx list-indices
- idx data import
- idx data status
- idx backtest run

验收标准：

- 命令行可以正常运行
- pytest 通过

## Task 008：添加 Streamlit dashboard 骨架

实现：

- index_platform/dashboard/app.py
- dashboard 页面：
  - 数据状态
  - 指数查看
  - 回测结果

验收标准：

- streamlit run index_platform/dashboard/app.py 可以运行
- dashboard 不包含核心策略逻辑

## Task 009：添加运行状态监控

实现：

- run state 文件
- run status 更新
- CLI monitor 命令
- dashboard run monitor 页面

验收标准：

- 回测写入 running / success / failed 状态
- monitor 可以读取状态